import logging

# Configure logging
logging.basicConfig(
    filename='/media/lucas/Files/2.Projetos/6.scrapping/data.log',  # Adjust the path as needed
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)